version: 1
confirmed: true
defaultEnvironment: environment_local
environments:
  - id: environment_local
    revision: 1
    mode: native
    requiredTools:
      - name: git
        versionRange: ">=2"
    validationProfiles: [host-default]
    networkProfile: none
    cacheProfiles: []
    requiredLicences: []
